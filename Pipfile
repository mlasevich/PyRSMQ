[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
test = 'python -m pytest --junitxml ./reports/results.xml --cov-config .coveragerc --cov=src .'

[dev-packages]
fakeredis = "1.7.1"
pytest-cov = "*"
pylint = "*"
mock = "*"

[packages]
redis = ">=3.0.0"

[requires]
